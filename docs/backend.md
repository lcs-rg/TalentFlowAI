# Backend

> Este documento foi consolidado em `architecture.md`.
>
> A organização por camada global (`controller/`, `service/`, `repository/`) descrita anteriormente neste arquivo está **obsoleta** e não deve ser seguida.
>
> Consulte `architecture.md` como fonte única de verdade sobre arquitetura de backend (Monólito Modular, organização por domínio, princípios de DDD pragmático).
>
> Este arquivo é mantido apenas para não quebrar links existentes.
